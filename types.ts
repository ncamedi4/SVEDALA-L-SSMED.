export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
}

export interface ContactPerson {
  name: string;
  phone: string;
  displayPhone: string;
}

export interface Partner {
  name: string;
}